spring.application.name=creator-settlement

# 초기 시드 (data.sql) 자동 실행
# defer 옵션으로 Hibernate 가 스키마를 만든 직후 data.sql 이 실행되도록 한다.
spring.jpa.defer-datasource-initialization=true
spring.sql.init.mode=always

